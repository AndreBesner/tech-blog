



<div class="container mt-5">
  <div class="row headerRow">
    <div class="col-8">
      <h2 class="blogTitle text-uppercase">{{name}}</h2>
    </div>
    <div class="col-4">
      <p class="blogCreate text-right">Created by {{user.name}} on {{format_date date_created}}</p>
    </div>
  </div>
  <p class="text-center blogDescTwo">{{description}}</p>
</div>

//section for if logged in to leave a comment
{{#if logged_in}}
<div class="container">

</div>
{{/if}}
//section comments on post 

<script src="js/comments.js"></script>
